/*class UI {
    constructor() {
        this.startGameText = game.add.text(phaser.config.width / 2, phaser.config.height / 2, 'Click to Start', {
            font: '30px Arial',
            fill: '#fff'
        });
        this.startGameText.setOrigin(0.5, 0.5);

        this.scoreText = game.add.text(10, 10, 'Score: 0', {
            font: '34px Arial',
            fill: '#fff'
        });
    }

    updateScoreText(newScore) {
        this.scoreText.setText('Score: ' + newScore);
    }

    showStartText() {
        this.startGameText.visible = true;
    }

    disableStartText() {
        this.startGameText.visible = false;
    }
}   */
/*
1 define platform
2 Place 1
3Get bouderin box, then add 1 so that no overlap
4limit distance from platform min|max
5limit width min|max
6Different X Y 
7limit height min|max
*/